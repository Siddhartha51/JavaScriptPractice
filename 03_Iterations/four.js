const obj ={
    js: 'javascript',
    cpp: 'c++',
    rb: 'ruby',
    swift: 'swift by apple'
}
for (const key in obj) {
    //console.log(`${key} shortcut is for ${obj[key]}`)
}

const array = ["djksn", "esefes", "sefs"]
for (const key in array) {
    console.log(array[key]);
    
}